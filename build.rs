// use spirv_builder::{SpirvBuilder, MetadataPrintout};

fn main() -> Result<(), Box<dyn std::error::Error>> {
    /* SpirvBuilder::new("shaders/main_shader", "spirv-unknown-vulkan1.0")
        .print_metadata(MetadataPrintout::Full)
        .build()?;   */  
    Ok(())
}